# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
ios/
# NOTE: android/ is committed (the Capacitor native project). Its build outputs
# (.gradle/, build/, local.properties, copied web assets) are ignored by the
# Capacitor-generated android/.gitignore, not here.

# Environment
.env
.env.local
.env.*.local

# IDEs/System
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tests
test-results/
playwright-report/
blob-report/
playwright/.local-browsers/

# Itch assets cache
.itch-cache/
src/assets/fetched/

# Agent-state derivable cache (directive.md + decisions.ndjson are tracked)
.agent-state/digest.md
.agent-state/cursor.md
.agent-state/.indexed-sha
.agent-state/.last-stop-sha
.agent-state/.last-stop-fingerprint
.agent-state/.last-compaction
.agent-state/batch.json
.claude/scheduled_tasks.lock

# Playtest screenshot artifacts
artifacts/
